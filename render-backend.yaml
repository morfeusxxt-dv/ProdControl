services:
  - type: web
    name: prodcontrol-backend
    env: docker
    plan: free
    rootDir: prodcontrol-backend
    dockerContext: .
    dockerfilePath: ./Dockerfile.prod
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: production
    healthCheckPath: /actuator/health
